# GET /admin/api/nodes — the fleet nodes with their inbounds.
get:
  operationId: nodesGet
  summary: List nodes
  security:
    - adminSession: []
  responses:
    '200':
      description: nodes
      content:
        application/json:
          schema:
            type: object
            required: [nodes]
            properties:
              nodes:
                type: array
                items:
                  type: object
                  required: [id, name, vpnHost, panelBaseURL, panelBasePath, inbounds]
                  properties:
                    id:
                      type: integer
                      format: int64
                    name:
                      type: string
                    vpnHost:
                      type: string
                    panelBaseURL:
                      type: string
                    panelBasePath:
                      type: string
                    inbounds:
                      type: array
                      items:
                        type: object
                        required: [id, name, port]
                        properties:
                          id:
                            type: integer
                            format: int64
                          name:
                            type: string
                          port:
                            type: integer
                          kind:
                            type: string
                            description: '"vless" (panel-managed) or "hysteria2".'
                          hysteria2:
                            type: object
                            description: >
                              Non-secret hysteria2 params (present only for kind=hysteria2).
                              The password is write-only and never returned.
                            properties:
                              obfs:
                                type: string
                              sni:
                                type: string
                              up:
                                type: string
                              down:
                                type: string
    '401':
      description: unauthorized
      content:
        application/json:
          schema:
            $ref: './common.yaml#/components/schemas/ErrorResponse'
